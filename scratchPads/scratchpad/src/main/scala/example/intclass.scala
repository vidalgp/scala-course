/*package idealized.scala

class Int {
	// overloading features are possible in scala!
	def + (that: Double): Double
	def + (that: Float): Float
	def + (that: Long): Long
	def + (that: Int): Int // Same for -, *, /, % 

	def << (cnt: Int): Int

	def & (that: Long): Long
	def & (that: Int): Long

	def == (that: Double): Double
	def == (that: Float): Float
	def == (that: Long): Long
}*/